import 'package:flutter/material.dart';

class DiscoverScream extends StatelessWidget {
  const DiscoverScream({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Discover Scream'),
      ),
    );
  }
}